# PDF ChatBot

## Steps

- set an environment variable to load models from an external HDD: **single-extract**
- extract text from an image: **single-extract**
- beautify the Go program by making it interactive - still a single generate request
- rename the GitHub repo
- query a PDF for information
  - we started by using an image
- edit a PDF
